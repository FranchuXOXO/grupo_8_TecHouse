<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ("../partials/head.ejs") %>
    <link rel="stylesheet" href="../css/styles-detalle.css">
</head>
<body>
    <%- include("../partials/header.ejs") %>
    <main>
        <div class="barra">
            <h2> <%= product.name %> </h2>
            <a href="/Carrito"> <button type="button"> Comprar </button> </a>

        </div>
             <article>
                <h2><%= product.name %></h2>
                <a href="/Detalle/<%= product.id %>">
                    <img src="/images/Products/<%= product.image %>" alt="<%= product.name %>" class="product_img">
                 </a>              
                    <div class="main-article">
                        <p class="price">Precio: $ <%= product.price %>
                        </p>
                        <h4 class="description"> <%= product.description %>
                        </h4>
                        <p class="colours">Colores: <%= product.colours %>
                        </p>
                        <p class="compatiblity">Compatible con: <%= product.compatibility %>
                        </p>
                    </div>
            </article>
      <div class="Edition-Delete">
            <a href="/<%= product.id %>/edit">   <button type="button"> Editar producto  </button> </a>
            <form action="/<%= product.id %>?_method=DELETE" method="POST">
                <button type="submit">Eliminar producto</button> 
            </form>
         </div> 
      
    </main>
    <%- include("../partials/footer.ejs") %>

</body>

</html>