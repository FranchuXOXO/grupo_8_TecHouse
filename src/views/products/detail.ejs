<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ("../partials/head.ejs") %>
    <link rel="stylesheet" href="../css/detail.css">
</head>
<body>
    <%- include("../partials/header.ejs") %>
    <main>
        <article>
            <div class="article-flex-container">
                <div class="titlePic">
                    <h2> <%= article.product_name %> </h2>
                    <a href="/Detalle/<%= article.id %>">
                    <img class="product_img" src="/images/Products/<%= article.product_image %>" alt="<%= article.product_name %>" ></a>  
                </div>            
                <div class="main-article">
                        <p class="price">Precio: $ <%= article.product_price %>
                        </p>
                        <h4 class="description"> <%= article.product_description %>
                        </h4>
                        <p class="colours">Colores: <%= article.product_colors.color %>
                        </p>
                        <p class="compatiblity">Compatible con: <%= article.product_compatibilities.device_name %>
                        </p>
                    <div 
                            <% if (locals.user && user.id_category=="1") {   %>
                            class="Show"
                            <% } else  {  %>
                            class="Edition-Delete"  
                            <%  } %>>
                            <a href="/<%= article.id %>/edit">
                                <button type="button" class="button-50"> Editar producto  </button> </a>
                            <form action="/<%= article.id %>?_method=DELETE" method="POST">
                            <button type="submit" class="button-50">Eliminar producto</button> 
                            </form>
                    </div>  
                </div>
            </div>
            <div class="button-flex-container">
                <a href="/cart">
                    <button type="button" class="button-50">
                        Comprar
                    </button>
                </a>
            </div> 
        </article>
    </main>
    <%- include("../partials/footer.ejs") %>

</body>

</html>